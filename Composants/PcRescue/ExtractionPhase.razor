@using NuitInfo.Rubeus.PcRescue.Models
@using NuitInfo.Rubeus.PcRescue.Services
@inject PcRescueGameService Game

<div class="phase-container">
    <div class="phase-header">
        <h1>Phase 2 - Extraction & Tri des composants</h1>
        <p class="phase-description">
            Extrayez les composants de l'ancien PC avec précaution,<br/>
            puis triez-les de manière écologique dans les bonnes poubelles.
        </p>
    </div>

    <div class="phase-content">
        <!-- Placeholder pour la zone d'extraction -->
        <div class="extraction-zone">
            <div class="placeholder-box">
                <h3>🔧 Zone d'extraction</h3>
                <p>Interface de mini-jeu QTE à venir</p>
                <p class="info-text">
                    Composants à extraire : @Game.State.Components.Count(c => c.State == ComponentState.InstalledOld)
                </p>
            </div>
        </div>

        <!-- Placeholder pour la zone de tri -->
        <div class="sorting-zone">
            <div class="placeholder-box">
                <h3>♻️ Zone de tri</h3>
                <p>Interface de tri écologique à venir</p>
                <p class="info-text">
                    Poubelles : Déchets / Recyclage / Réutilisation
                </p>
            </div>
        </div>

        <!-- Bouton temporaire pour passer à la phase suivante -->
        <div class="action-zone">
            <button class="btn-primary" @onclick="GoToShop">
                Aller au Shop →
            </button>
        </div>
    </div>
</div>

<style>
    .phase-container {
        text-align: center;
    }

    .phase-header h1 {
        color: #667eea;
        margin-bottom: 15px;
    }

    .phase-description {
        color: #666;
        font-size: 1.1em;
        line-height: 1.6;
        max-width: 600px;
        margin: 0 auto 30px;
    }

    .phase-content {
        margin-top: 40px;
    }

    .extraction-zone,
    .sorting-zone {
        margin-bottom: 30px;
    }

    .placeholder-box {
        background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
        border: 2px dashed #667eea;
        border-radius: 12px;
        padding: 40px 30px;
        max-width: 600px;
        margin: 0 auto;
    }

    .placeholder-box h3 {
        margin: 0 0 10px 0;
        color: #667eea;
    }

    .placeholder-box p {
        margin: 5px 0;
        color: #666;
    }

    .info-text {
        font-weight: bold;
        color: #764ba2;
        margin-top: 15px;
    }

    .action-zone {
        margin-top: 40px;
    }

    .btn-primary {
        background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
        color: white;
        border: none;
        padding: 15px 40px;
        font-size: 1.2em;
        border-radius: 8px;
        cursor: pointer;
        font-weight: bold;
        transition: transform 0.2s, box-shadow 0.2s;
        box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .btn-primary:hover {
        transform: translateY(-2px);
        box-shadow: 0 6px 12px rgba(0, 0, 0, 0.15);
    }

    .btn-primary:active {
        transform: translateY(0);
    }
</style>

@code {
    /// <summary>
    /// Passer à la phase Shop
    /// (temporaire - la vraie logique viendra avec les mini-jeux)
    /// </summary>
    private void GoToShop()
    {
        // Pour l'instant, on passe directement au shop
        // Dans l'étape 3, cette transition ne se fera qu'après
        // avoir extrait et trié tous les composants
        Game.State.Phase = GamePhase.Shop;
    }
}
